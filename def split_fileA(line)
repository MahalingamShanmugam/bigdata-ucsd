
# This solution is for Week 5 Spark Exercise
# Below code is for splitting words in file A
# copy the below and paste is pyspark shell command

def split_fileA(line):
...     line = line.strip()
...     key_value = line.split(",")
...     word = key_value[0]
...     count = int(key_value[1])
...     return(word, count)

